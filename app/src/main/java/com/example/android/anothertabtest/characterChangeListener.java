package com.example.android.anothertabtest;

public interface characterChangeListener {
    // These methods are the different events and
    // need to pass relevant arguments related to the event triggered
     void handleCharacterChange(com.example.android.anothertabtest.Character character);
//    public void onObjectReady(String title);
    // or when data has been loaded
//    public void onDataLoaded(SomeData data);
}
